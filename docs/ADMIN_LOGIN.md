# Admin Login Credentials

For accessing the admin dashboard:

- **URL**: http://localhost:3000/login  
- **Email**: `admin@skinlab.com`
- **Password**: `admin123`

This account is automatically created during database initialization (`npm run init-db`).

**Security Note**: Change the default password in production by modifying `data/init.ts` before running the init script.
